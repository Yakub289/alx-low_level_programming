#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: the int to check.
 * Return: the absolute value of int.
 */

int _abs(int n)
{
	int number = 0;

	if (n >= number)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
