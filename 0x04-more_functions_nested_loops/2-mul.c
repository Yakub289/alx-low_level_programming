#include <stdio.h>
#include "main.h"

/**
* mul - Multiplies two integers.
* @a: first integer.
* @b: second integer.
* Return: Multipliers of a and b (success).
*/

int mul(int a, int b)
{
	return (a * b);
}
